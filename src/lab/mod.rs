//! Research instruments, behind the `lab` feature.
//!
//! These are not engines: they are the working surface of open research
//! lanes (the √2-cancellation lower bound). They carry their own
//! soundness arguments, they move faster than the pillars, and nothing
//! in `classical` or `quantum` depends on them.

pub mod odd;
pub mod oddmin;
